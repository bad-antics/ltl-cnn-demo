// examples/demo.ltl — runnable demo for ltl_cnn_demo

import std::io
use ltl_cnn_demo as lib

fn main() {
    io::println("ltl_cnn_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}